# Augustus API key — get from https://dashboard.augustus.com (Test Mode → Integration)
AUGUSTUS_API_KEY=your_augustus_sandbox_key

# Anthropic API key — get from https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_key

# Set to "production" to use live API (default: sandbox)
# AUGUSTUS_ENV=production
